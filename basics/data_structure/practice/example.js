
const foo = 42; // foo is a number
const result = foo + "2"; // JavaScript coerces foo to a string, so it can be concatenated with the other operand
console.log(result); // 421

type = typeof(result)
console.log(type)
